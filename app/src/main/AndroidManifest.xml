<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.towmec.mainproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".editprofile"></activity>
        <activity android:name=".login2" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".setdriver" />
        <activity android:name=".helpdriver" />
        <activity android:name=".paydriver" />
        <activity android:name=".HistoryPage" />
        <activity android:name=".CustomerMapActivity" />
        <activity
            android:name=".splash"
            android:theme="@style/Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".Driverlogin" />
        <activity android:name=".learntips" />
        <activity
            android:name=".tripPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity
            android:name=".paymentPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity
            android:name=".helpPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity
            android:name=".promoPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity
            android:name=".setPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity
            android:name=".legalPage"
            android:parentActivityName=".CustomerMapActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CustomerMapActivity" />
        </activity>
        <activity android:name=".DriverSignup" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".DriverMapsActivity"
            android:label="@string/title_activity_driver_maps" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="ocr" />

        <activity android:name=".DriverSignUp2" />
        <activity android:name=".DriverSignup3" />
        <activity android:name=".DriverSignup4" />
    </application>

</manifest>